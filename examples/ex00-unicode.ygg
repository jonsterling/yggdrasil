(computad booleans
  (sign types
    (∂ 𝔹 type))
  (sign terms
    (∂ ⊥ 𝔹)
    (∂ ⊤ 𝔹)
    (∂ ¬ (→ 𝔹 𝔹))
    (∂ ∧ (→ [𝔹 𝔹] 𝔹)))
  (sign rules
    (∂ ¬/⊥ (→ (¬ ⊥) ⊤))
    (∂ ¬/⊤ (→ (¬ ⊤) ⊥))
    (∂ ∧/⊥/⊥ (→ (∧ ⊥ ⊥) ⊥))
    (∂ ∧/⊥/⊤ (→ (∧ ⊥ ⊤) ⊥))
    (∂ ∧/⊤/⊥ (→ (∧ ⊤ ⊥) ⊥))
    (∂ ∧/⊤/⊤ (→ (∧ ⊤ ⊤) ⊤))
    (∂ ∧/η (λ [(∂ x 𝔹) (∂ y 𝔹)] (∧ x y))))
  (@@analyze 𝔹)
  (@@analyze (¬ ⊥))
  (@@analyze (¬ ⊤))
  (@@analyze (∧ ⊥ ⊥))
  (@@analyze (∧ (∧ ⊤ ⊤) ⊥))
  (@@analyze (∧ (∧ ⊤ ⊤) ⊤))
  (@@analyze (∧ (∧ ⊤ ⊤) (¬ ⊥)))
  (@@analyze ∧/η)
  (@@analyze (λ (∂ x 𝔹) (¬ x)))
  (@@analyze (λ [(∂ x 𝔹) (∂ y 𝔹)] (∧ x y)))
  (@@analyze (λ (∂ x 𝔹) (λ (∂ y 𝔹) (∧ x y)))))
